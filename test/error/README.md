# C and C++ error handling to/from Fortran

The C++ main() using Fortran library and Fortran main using C++ library examples show C++ error handling.

The C main() using Fortran library and Fortran main using C library show that the library can exit the main program on error.

This C approach trivially also works in C++.
Here we instead show how C++ error handling can work with Fortran.
